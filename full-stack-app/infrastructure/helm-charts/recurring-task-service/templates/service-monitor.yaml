{{- if .Values.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "recurring-task-service.fullname" . }}
  labels:
    {{- include "recurring-task-service.labels" . | nindent 4 }}
  namespace: monitoring  # Usually deployed in a monitoring namespace
spec:
  selector:
    matchLabels:
      {{- include "recurring-task-service.selectorLabels" . | nindent 6 }}
  endpoints:
  - port: http
    interval: 30s
    path: /metrics
    honorLabels: true
{{- end }}